# Netlify/Cloudflare Pages SPA fallback

# Serve static files directly
/sitemap.xml    /sitemap.xml    200
/robots.txt     /robots.txt     200

# Redirect all other requests to index.html to support client-side routing
/*    /index.html   200
